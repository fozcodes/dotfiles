function mkdircd () { mkdir -p "$@" && eval cd "\"\$$#\""; }

function npms () { npm install --save "$@"; }
function npmsd () { npm install --save-dev "$@"; }

function mixnew () { mix new "$@" && eval cd "\"\$$#\""; }

function phnew () { mix phoenix.new "$@" && eval cd "\"\$$#\"";}
